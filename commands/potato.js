const Discord = require("discord.js")

module.exports.run = async (bot, message, args) => {
    return message.reply("njam jam :yum: :drool:")
}

module.exports.config = {
    name: "potato",
    aliases: []
}